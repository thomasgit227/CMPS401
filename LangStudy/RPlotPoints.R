# Example of how to plot points inside of R

#Sample Data Points Assigned to Two Variables
x <- c(.5, 1, 1.5, 2, 2.5)
y <- c(.7, 3.4, 7.2, 12.4, 20.1)

#Plot Function, (Note: pch refers to the type of dot formed)
plot(x, y, pch = 16, col = "black")